import { SearchWorkoutPipe } from './search-workout.pipe';

describe('SearchWorkoutPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWorkoutPipe();
    expect(pipe).toBeTruthy();
  });
});
